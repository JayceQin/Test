package codesafe.automation.drivers.selenium.views.reportManagePage;

import codesafe.automation.drivers.selenium.views.BaseView;
import org.openqa.selenium.WebDriver;

public class ReportManageView extends BaseView {
      
      /**
       * Intentionally empty constructor method to support instantiation.
       *
       * @param acDriver
       */
      public ReportManageView(WebDriver acDriver) throws Exception {
            super(acDriver);
      }
}
